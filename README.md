# www.kirishikistudios.com Website

code is taken from https://github.com/smth/jekyll-one

## local development

```ruby
gem install jekyll
jekyll serve
```

jekyllのインストールで失敗するとき（MacOS M1)

```shell
gem install eventmachine -v '1.2.7' -- --with-openssl-dir=$(brew --prefix libressl)
```
